[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/api/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
# Smoke: ask for enough RAM for JVM engine + a few bots
# (set memory in Railway dashboard / CLI if plan allows)
