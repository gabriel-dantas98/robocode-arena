{
  "name": "robocode-arena-e2e",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:record": "playwright test --config=playwright.config.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.1"
  }
}
